<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Set Board 4-3. Create mapper file  -->
<mapper namespace="blog">
	<select id="selectAll" resultType="BlogVo">
	<![CDATA[
		SELECT b.no, b.title, b.hit, b.regdate, b.member_no memberNo, m.name memberName
		FROM board b, members m
		WHERE b.member_no = m.no
		ORDER BY regdate DESC
	]]>
	</select>
	
	<select id="selectByUserNo" parameterType="string" resultType="BlogVo">
	<![CDATA[
		SELECT userNo, blogTitle, logoFile FROM blog
		WHERE blog.userNo=#{no}
	]]>
	</select>
	
	<!-- Set Board 5-7-7. Write SQL Query -->
	<insert id="insert" parameterType="BlogVo">
	<![CDATA[
		INSERT INTO board (no, title, content, member_no)
		VALUES (seq_board_no.NEXTVAL, #{title}, #{content}, #{memberNo})
	]]>
	</insert>
	
	<update id="updateBlogTitle" parameterType="BlogVo">
	<![CDATA[
		UPDATE blog SET blogtitle = #{blogTitle} WHERE userNo = #{userNo}
	]]>
	</update>

</mapper>